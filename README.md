AmexFileParser
A C# console application that parses AMEX fixed-width settlement files and extracts structured settlement data across multiple report sections.
Built to replace manual data extraction from AMEX reports with a programmatic, structured pipeline.

